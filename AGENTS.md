# 中国惯用语大全 · 项目上下文（AGENTS.md）

此目录是一个**纯静态中文“惯用语 + 成语”查询网页**，无后端、无构建步骤，双击 `chinese_idioms.html` 即可在浏览器使用（file:// 直接打开正常）。

## 文件清单与职责
- `index.html`：唯一入口页（曾用名 `chinese_idioms.html`），按顺序加载 `data.js` → `chengyu_full.js` → `script.js`，样式 `style.css`。
- `data.js`：**人工精选数据，勿轻易改动内容**。声明 `var HABIT_DATA`（惯用语 180 条）、`var IDIOM_DATA`（成语 121 条）。
- `chengyu_full.js`：**全量自动生成数据**。声明 `var HABIT_BULK_DATA`（惯用语 1382 条）、`var IDIOM_BULK_DATA`（成语 29502 条）。由开源语料 chinese-xinhua `idiom.json`（30895 条）生成。
- `script.js`：全部交互逻辑（合并去重、搜索、筛选、增量渲染）。**除非必要不要改**。
- `style.css`：界面样式（顶部开关、词条卡片、频率标签颜色等）。

全站合计 **31024 条**：惯用语 1521（常用182 / 较常用15 / 一般1324），成语 29503（常用1056 / 较常用1173 / 少见27274）。
合计按常用度：常用 1238 · 较常用 1188 · 一般 1324 · 少见 27274。

## 数据行格式（关键约定）
每一行数组：`[词条, 释义, 例句1, 例句2, 常用度]`
- 精选数据行只有前 4 项，读取时 `freq` 取不到就默认 `"常用"`。
- 全量数据行第 5 项为 `"常用" / "较常用" / "一般" / "少见"` 之一，第 4 项恒为空串。
- 词条分类不看长度规则，由所在数组决定：
  - `*_HABIT*` 数组 → 词类标签“惯用语”；
  - `*_IDIOM*` 数组 → 词类标签“成语”。

## 生成规则（曾用于构建 chengyu_full.js，原 tools 已删）
- 语料：https://github.com/pwxcoo/chinese-xinhua 的 `data/idiom.json`，30895 条，字段含 `word / explanation / example / derivation`。
- 清洗：`example` 取 `★` 前段、把 `～` 替换为词条、超 90 字截断加 `…`、占位文本“无 / 略”清空；释义去多余空白。
- 归类：去括号与标点后**四字 → 成语**；三字及以上非四字（如 安乐窝、八九不离十）→ 惯用语。
- 常用度分级：命中精选词表 tier1（常用 1025 个）→ `常用`；tier2（较常用 2135 个，去重后仅 tier2 命中）→ `较常用`；否则惯用语默认 `一般`、成语默认 `少见`。
- 去重键：`词类 + 词条去掉（）、空格后`；精选数组先加载，重复时精选版优先。

## 踩过的坑（重要，避免再次出错）
1. **顶层声明必须用 `var`，不能用 `const`**：浏览器里顶层 `const` 不会挂到 `window`，而 `script.js` 用 `window.HABIT_DATA` 等读取，会导致页面显示“当前词库暂无内容”。用 `var` 声明数组即可。
2. 曾有过不存在的 `habit_extra.js` 引用与“四档+缺档”方案，均已废弃；当前只有上面 5 个文件，`data.js` + `chengyu_full.js` 两层即可。
3. 所有文件均为 UTF-8 无 BOM。Windows 上 PowerShell 控制台默认 GBK，中文会显示乱码，务必用 `node` 读写或用 `[Console]::OutputEncoding = UTF8`；写文件用 Node `fs.writeFileSync(path, txt, "utf8")` 最稳妥。
4. 修改本目录文件前，Codex 会自动读取本 `AGENTS.md`；追加/删除文件后请同步更新本文件。

## 会话历史（供续接上下文）
1. 用户最初要求：在原有“惯用语大全”网页基础上**扩充到尽量全**（成语也纳入），并**按常用度加分类按钮**。已交付：词类（惯用语/成语）+ 常用度（常用/较常用/一般/少见）双筛选、搜索、释义/例句、悬停开关、增量加载全部保留。
2. 修复过“当前词库暂无内容”：原因即上面坑 1（const 不挂 window）。
3. 用户要求删除 `tools` 文件夹（含构建脚本与 10MB 语料）→ 已删；如需重新生成全量数据，需按“生成规则”重新下载 `idiom.json` 与两张词表。
4. 原入口页 `中国惯用语大全.html` 已改名为 `chinese_idioms.html`；本文件夹由 `AiOutPut` 改名为 `ChineseIdiomsWeb`（即当前目录）。

5. 修复“当前筛选 / 搜索计数不更新”bug：根因在 `script.js` 的 `renderMoreArea()`（“加载更多”容器被 `textContent=""` 清空后再访问 `moreBtn.parentNode` 会抛错、末段容器自移除也抛错），已重写为“先摘旧容器、需要时重建”，并让 `drawMore()` 按固定引用插卡、修复首屏第一条错位；仅改 `script.js`。
6. 应约新增人工词条 `撞南墙`（惯用语，精选 `data.js`，4 字段默认“常用”）：源语料未收录、此前只出现在例句中；已同步更新统计。
7. 应约排查全库释义：释义中以“同 / 犹言 / 亦作 / 引号”等形式提及却未收录的词多为冷僻文言变体（铢积寸累、局高天蹐厚地等），未补录；真正的缺口是源语料未收录的日常惯用语。新增 19 条常用惯用语到精选 `data.js`（默认“常用”）：不撞南墙不回头、出洋相、兜圈子、马大哈、和稀泥、耍花招、背黑锅、吃闭门羹、拍胸脯、露一手、有两下子、留后路、拉下水、摇钱树、挡箭牌、绊脚石、炒冷饭、唱反调、揭不开锅。已同步统计。
8. 微调 `script.js` 的 `updateGuide()`：词类、常用度均为“全部”且无搜索词时，下方提示不再为空，改为始终显示“当前筛选：全部 / 全部，共 N 条。”（单维筛选时另一维也显示“全部”，如“惯用语 / 全部”）；有搜索词时保持原“找到 N 条与「q」相关…”样式。统计不变。
9. 修复首次打开页面时“词类”的“全部”按钮未高亮：给 `chinese_idioms.html` 中该按钮补上默认 `active` 类（此前只有“常用度”的“全部”带 `active`）。仅改 HTML，统计不变。
10. 入口页由 `chinese_idioms.html` 改名为 `index.html`；本文件与打开方式说明已同步。
11. 移动端优化 + 锚定修复：手机（`(hover: none) and (pointer: coarse)` 判定）下滑隐藏顶栏标题、上滑恢复，桌面不受影响；手机在“释义隐藏”模式下点按词条显隐释义（同屏只展开一个），桌面仍为悬停/聚焦；切换“释义全部显示”时先记录视口最上方词条，再用高度差补偿滚动，保证该词条位置不跳变（开关不再整表重建）。仅改 `index.html`/`style.css`/`script.js`。
12. 新增深色/浅色主题：顶栏按钮切换，默认深色，存 `localStorage`（键 `cy_theme_v1`），浅色主题通过追加的 CSS `:root[data-theme="light"]` 覆盖实现。
13. 新增本地收藏：每张卡片右上角 ☆ 收藏按钮；收藏存 `localStorage`（键 `cy_favs_v1`，值为 `词类+去括号词条` 键的 JSON 数组）；顶栏“☆ 收藏”按钮切换“仅看收藏”视图；首次收藏弹出提示（键 `cy_fav_warned_v1`）提醒在常用浏览器打开以免数据丢失。
14. 本轮修复收藏与锚定细节：修复“首次收藏没有提示”——`maybeWarnFirstFav()` 改为会话级去重（`warnShownThisSession`），空收藏时首次添加（或该浏览器从未被提示过）必定弹气泡提醒“用常用浏览器打开以免丢失”，清空收藏后再添加也会再次提示，同一会话内重复添加不再反复弹。修正“释义全部显示”切换时的锚定规则：以“中国惯用语大全”标题矩形（`.brand-copy`）为基准，标题矩形被隐藏时锚定视口内最上方（顶栏之下）的卡片，标题矩形可见时锚定其正下方对应的那张卡片；给 `index.html` 顶栏补 `id="topbar"`。收藏栏底部新增“清除浏览数据”按钮与确认弹窗（取消关闭、确定才清空收藏并重渲染），样式含 `.fav-tools`/`.modal-*` 与浅色主题适配。仅改 `index.html`/`script.js`/`style.css`，统计不变；已用 Edge headless 自测通过（首次收藏提示、收藏视图、清除确认、主题切换、标题可见/隐藏两种锚定均稳定不跳位）。
15. 顶部按钮文案/样式微调：收藏切换按钮由“收藏”改名“收藏夹”（平时 `☆ 收藏夹`、收藏视图 `★ 收藏夹 N`，见 `script.js` 的 `refreshFavButton()`）；主题切换按钮去掉文字、只保留 ☀/☾ 图标，并改为 36px 圆形按钮（新增 `.top-btn.icon-btn` 样式）。仅改 `index.html`/`script.js`/`style.css`，统计不变；已用 Edge headless 自测（按钮文字、图标随主题切换、`border-radius:50%` 圆形均符合）。
16. 首次收藏提示改为小弹窗：`script.js` 抽出通用 `openModal(title, body, buttons)`（复用清除收藏夹数据的 `.modal-mask`/`.modal-box` 样式），首次收藏弹“收藏提示”（含本浏览器保存/换浏览器丢失提醒，按钮“知道了”），同一会话只弹一次；“清除浏览数据”确认框同步改为由 `openModal` 生成（取消/确定清除）。原 `showToast()` 及 toast 调用已移除（toast 相关 CSS 保留未用）。仅改 `script.js`，统计不变；Edge headless 自测通过。
17. 分页数量调整：每页数量 `PAGE` 由 400 改为 50，首次加载与每次点击“加载更多”均按 50 条递增渲染（`script.js`，其余逻辑不变）。仅改 `script.js`，统计不变；Edge headless 验证初始 50、两次“加载更多”后 100/150。
18. 移动端下滑收起范围调整：不再只折叠 `.brand-copy` 文字，改为隐藏“中国惯用语大全”所在的整块 `.brand` 容器（含“惯”字图标与副标题，`max-height:0` + 透明度过渡），`body.title-hide` 时顶栏只保留右侧的释义开关/收藏夹/主题按钮，上滑或回到顶部恢复。仅改 `style.css`，统计不变。
19. 修复手机端功能未生效：原移动端判定只看 `(hover:none) and (pointer:coarse)`，很多手机/浏览器会判失败，导致“下滑隐藏标题”和“点击卡片显隐释义”都失灵。现判定放宽为：coarse 媒体查询命中，或移动 UA，或（有触摸且视口 ≤1024px），或无触摸的窄窗口 ≤760px；仅桌面宽屏不受影响。同时手机端首屏默认“释义收起”（加载即 compact，点击词条显示释义、再点任意区域包括释义文本即隐藏，同屏只展开一个）；桌面仍为悬停/聚焦。仅改 `script.js`，统计不变；Edge headless 在模拟 coarse 与 500px 窄窗两种场景验证均通过。
20. 顶栏收起逻辑重做：①范围改为整条 `.topbar`（含“中国惯用语大全”、释义开关、收藏夹、主题按钮三个控件一起隐藏），用 `transform:translateY(-100%)` 移出视口（不动布局，避免内容跳动）；②判定方向按“手指上滑→隐藏、手指下滑→显示”，并把阈值改为带滞回（隐藏需 dy>8、显示需 dy<-4，4px 内抖动不翻转），消除“鬼畜”；回顶（y≤4）始终显示。仅改 `style.css`/`script.js`，统计不变；Edge headless（500px 窄窗、禁用过渡）验证整条顶栏与三个按钮均移出视口、下滑恢复、微抖动不翻转。
21. 隐藏模式释义显隐重做（修复三个 bug）：①手机 compact：点一次显示释义、再点一次（含释义文本区域）隐藏，点其它卡片自动切换（原逻辑保留，去掉了点释义区不响应的限制）；②③桌面 compact：不再用 CSS `:hover/:focus-within` 显示，改为 JS 控制 `.open`——`pointerenter` 显示、`pointerleave` 隐藏、`mousedown` 点击立即隐藏并解除卡片内聚焦（修复“点击后光标离开不隐藏”的 bug），键盘 `focusin/focusout` 仍可显示/隐藏。仅改 `style.css`/`script.js`，统计不变；Edge headless 验证桌面 1280px 与手机 500px(coarse) 两种场景均通过。## 常用操作
- 打开网页：浏览器打开 `Html/ChineseIdiomsWeb/index.html`（或直接双击）。
- 若浏览器缓存旧版：`Ctrl+F5` 强刷。
- 顶部右侧开关：关闭后释义/例句默认隐藏，鼠标悬停/聚焦词条时显示。
